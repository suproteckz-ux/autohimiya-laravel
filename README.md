# Autohimiya Laravel

Clean Laravel 12 repository candidate for Autohimiya.kz production deployment through Plesk.

This repository is intended to contain application source code only. Production `.env`, uploaded media, runtime cache, logs, Composer vendor files and Node dependencies must remain outside Git.
